# Implementation Plans

Generated by the `improve` skill on 2026-07-27, audited against commit `86764a9`
(v5.11.47). Execute in the order below unless dependencies say otherwise. Each
executor: read the plan fully before starting, honor its STOP conditions, and
update your row when done.

Baseline measured on `86764a9`, for drift detection:

- `make test` → `116 Tests 0 Failures 0 Ignored`
- `make clean && make` → zero warnings under `-Wall -Wextra -pedantic`
- `make test-valgrind` → exit 0

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Report malformed timestamp rows instead of dropping them silently | P1 | S | — | DONE |
| 002 | Add end-to-end tests for the timestamp-mode input parser | P1 | S–M | 001 | DONE |
| 003 | Make the header documentation in `tikhonov.h` and `savgol.h` true | P2 | S | — | DONE |
| 004 | Make the Tikhonov output derivative second-order on non-uniform grids | P2 | S | — | DONE |

**001** — executed, reviewed, and **merged into `main` as `05cee50`**
(fast-forward) on 2026-07-27. Branch `advisor/001-report-malformed-timestamp-rows`
retained; its worktree has been removed.

All eight done criteria were re-verified independently by the reviewer, plus two
extra checks: a file with only non-numeric skips produces byte-identical output to
v5.11.47 (the specific regression risk named in the plan's maintenance notes), and
normal-mode output is unchanged. Byte-identity was checked cross-version against
the v5.11.47 binary rather than against the executor's own baseline files.

Worth recording: the first dispatch **stopped correctly** on the plan's drift
check, because the worktree was created from the remote-tracking branch, two
commits behind the plan's baseline. That also exposed a defect in the plan itself
— it used `tests/test_data.dat` for its byte-identity baseline, a file that is
untracked and therefore absent from any worktree. Fixed to use the tracked
`test_data.txt` plus a generated fixture before re-dispatching.

**002** — executed, reviewed, and **merged into `main` as `ce6016e`**
(fast-forward) on 2026-07-27. Branch `advisor/002-timestamp-parser-e2e-tests`
retained; its worktree has been removed. Suite goes 116 → 123, zero compiler
warnings, valgrind clean, `parser.c` untouched.

The regression guard was mutation-tested by the reviewer: reverting `parser.c` to
its pre-001 state (`git checkout 86764a9 -- parser.c`) in a disposable worktree
makes `test_parser_ts_malformed_row_is_reported` fail with
`Expected 2 Was -1`, while the other six stay green. The guard has teeth and the
other tests do not silently depend on 001.

Known nit, not blocking: the comment above
`test_parser_ts_malformed_row_is_reported` ends "...not silently included with
garbage x". That is backwards — before 001 the row was silently *excluded*, not
included. The test itself is correct; only the comment misstates the bug. Worth a
one-line fix whenever that file is next touched.

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale)

## Dependency notes

- **002 requires 001** because 002's test `test_parser_ts_malformed_row_is_reported`
  asserts on the `# Skipped N data row(s) with malformed timestamp in column M`
  message that 001 introduces. Running them in the other order leaves `make test`
  red between commits, which `CLAUDE.md` forbids ("Do not commit without
  `make test` passing").
- 001 bumps `revision.h` to v5.11.48; 002 does not bump — test-only commits in
  this repo carry no version (see `902d74e`, `a3be811`).

## Audit findings not yet planned

Verified and ranked, but no plan written. Listed so they are not re-derived.

| Finding | Category | Effort | Note |
|---|---|---|---|
| ~~`tikhonov.h` header documentation is actively wrong (TK6)~~ | docs | S | **Planned as 003.** Re-verification found 7 defects, not the 4 originally recorded — see below. |
| CLI numeric args parsed with `atoi`/`atof` (B2) | bug | S | `-p abc` silently yields degree 0; `-n 99999999999999` is signed-overflow UB (observed: 276447231). `parser.c` already uses `strtod` + `endptr` correctly — the CLI does not. Open since v5.11.22. |
| No CI configuration | dx | S | No `.github/`, `.gitlab-ci`, or `.travis`. The `CLAUDE.md` hard rule "do not commit without `make test` passing and no new valgrind leaks" is enforced only by discipline. |
| ~~Tikhonov output derivative is 1st-order on non-uniform grids (TK5)~~ | bug | S | **Planned as 004.** Re-analysis sharpened it — see below. |
| `-l auto` writes 26 diagnostic lines into the data stream, 19 with UTF-8 `λ` (B8+B9) | tech-debt | S–M | Measured on 40 points. `help()` advertises Unix-filter use (`cat data \| smooth`); the rest of the program is ASCII. |

**003** — executed, reviewed, and **merged into `main` as `d57a778`**
(fast-forward) on 2026-07-27. Branch `advisor/003-fix-header-doc-drift` retained.
Comments only; `git diff -- '*.c'` empty, and program output on three method
invocations is byte-identical to `ce6016e`.

The review compiled both header examples independently of the executor's
extractor. The `tikhonov.h` example builds with zero compiler output and runs;
the `savgol.h` snippets build and **behave as the header claims** — uniform grid
returns a result, non-uniform returns NULL. The reviewer also confirmed the
compile-line fix is load-bearing rather than cosmetic: building the same example
without `grid_analysis.c` fails with 3 undefined references.

All eleven "do not touch" claims were re-grepped and are intact.

### TK6 re-verification, 2026-07-27 — wider than recorded

Planning 003 turned up three defects the original TK6 entry did not have. Recorded
here because the audit docs still describe TK6 as a four-item finding.

- **New, in `tikhonov.h`**: the example declares `TikhonovResult *result` twice in
  one scope (`:101` and `:105`) — a redefinition error independent of the missing
  `grid_info`. And the compile line (`:122`) omits `grid_analysis.c`, which the
  corrected example needs; without it the link fails.
- **New, in `savgol.h` — outside TK6 entirely**: `:73` and `:79` call
  `analyze_grid(x, 6, 0)` with three arguments. The real signature takes two
  (`grid_analysis.h:36`); `store_spacings` was removed, as `revision.h:98`
  records. Same defect class, different header, never recorded anywhere. Folded
  into 003 so the class is closed at once.
- Worth knowing: `savgol.h:98,101` documents Tikhonov's API **correctly**,
  `grid_info` and all — better than `tikhonov.h` documents its own. The
  cross-reference survived the drift; the module's own example did not.

Nothing prevents this recurring: headers are prose and the compiler never reads
them. 003 verifies by extracting the examples and compiling them, but that is a
one-off gate, not a standing one. If a third example rots, add "grep the headers
when you change a public signature" to the `smooth-dev-tasks` skill.

### TK5 re-analysis, 2026-07-27 — CV is the wrong predictor

Measured while planning 004. The audit framed TK5 as "1st-order on non-uniform
grids", which is true but misleading about *which* non-uniform grids.

The leading error is $\frac{h_r-h_l}{2}u''$ — proportional to local **asymmetry**,
not to overall spacing variation. Measured, $\lambda=0$ so the smoothing is exactly
the identity and only the derivative is under test:

| mesh | CV | clusters | improvement from the fix |
|---|---|---|---|
| alternating 0.1/0.4 | 0.608 | 0 | **22x** |
| geometric grading r=1.02 | 0.275 | 0 | 1x |
| geometric grading r=1.10 | 0.622 | 0 | 1x |
| geometric grading r=1.30 | 0.877 | 0 | 1x |

Two meshes at essentially the same CV (0.608 vs 0.622) differ by a factor of 22 in
how much the fix helps. On a graded mesh $h_r-h_l$ is comparable to the corrected
formula's own truncation error, so the two formulas meet.

Consequences worth carrying forward:

- Any future decision keyed on "is this grid bad for derivatives" should use local
  asymmetry $|h_r-h_l|/h$, **not** `GridAnalysis.cv`.
- `grid_analysis.c`'s cluster detector does not catch this either: it looks for a
  1:10 spacing ratio, and the 1:4 alternating mesh above reports `clusters: 0`
  while being the worst case for the derivative.
- The **boundary** derivative is first-order even on a uniform grid (two-point
  one-sided), measured 14x improvement there. That part of TK5 was never recorded
  and has nothing to do with non-uniformity.

## Findings considered and rejected

Recorded so nobody re-audits them.

- **TK3 — L-curve corner detection is triply fragile**: dead finding. The L-curve
  method and the `n > 20000` branch that reached it were deleted in `tikhonov.c`
  V5.4 (2026-07-26). `doc/audit-tikhonov.md` should be updated to retire it.
- **TK7 — redundant solves in the auto-lambda path**: measured, not worth doing.
  On 100k points, `-m 2 -l auto` takes 0.465 s versus 0.210 s for `-l 0.1` — about
  0.25 s of redundant work, much of the baseline being I/O. Its first bullet (the
  `n > 20000` GCV+L-curve double solve) is dead along with TK3.
- **TK4 — exact GCV trace**: a Hutchinson estimator or band-factorisation diagonal
  is a large intervention justified only if auto-lambda precision is a real
  constraint. Nothing currently depends on it.
- **`polyfit.c:244` — `sing_vals[effective_rank - 1]`**: verified safe. The
  Vandermonde matrix always has an all-ones column (x is strictly increasing), so
  `s[0] > 0` and `rank >= 1`; the index can never be `-1`.
- **Security**: no findings. Offline CLI with no network, shell execution, or
  credentials. The bounds handling in `parser.c` was re-read: `MAX_LINE`
  truncation detection, `MAX_COLS` overflow detection on both branches, and
  `strtod` + `endptr` + `isnan`/`isinf` validation all hold.
- **Performance**: no findings worth acting on. Whole-program timings are
  dominated by I/O at realistic sizes.
- **B3 — untracked scratch files in the working tree**: real but a one-line
  `.gitignore` edit; a plan would cost more than the fix.

## Not audited

`tests/unity*` (vendored third-party), `README.md`/`README.pdf` content, and the
untracked Python helpers (`benchmark*.py`, `plot_*.py`, `filter*.py`), which sit
outside the build.
